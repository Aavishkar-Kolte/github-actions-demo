name: Hello World Workflow

on: 
  push:
    branches:
      - master

jobs:
  hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: echo hello world 
        run: echo "Hello World!"
        shell: bash


